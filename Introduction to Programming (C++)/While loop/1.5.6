#include <iostream>

int main() {
    int N(0), i(0), sum(0);
    std::cin >> N;
    sum = i;
    while (N != 0) {
        i = N;
        sum = sum + i;
        std::cin >> N;
    }
    std::cout << sum;
}
