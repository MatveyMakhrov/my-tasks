#include <iostream>
 
int main() {
	int m(0), n(0), a1(2), a2(1), k(0); // a1 это длина доминошки, а a2 это ширина доминошки
	std::cin >> m >> n;
	k = m * n;
	int s;
	s = a1 * a2;
	int q;
	q = k / s;
	std::cout << q;
}
