#include <iostream>
#include <string>

int main() {
    int t(0), n(0);
    char c(0);
    std::cin >> t;
    std::string s;
    for (int i(0); i < t; i++) {
        std::cin >> n >> c >> s;
        for (int j(0); j < n; j++) {

        }

    }
}
